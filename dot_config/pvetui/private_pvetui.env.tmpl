# pvetui credentials — injected at runtime via 1Password.
# Loaded by the `pvetui` shell alias: `op run --env-file=... -- pvetui`.
# Keep secrets OUT of config.yml so pvetui's auto-encrypt never rewrites it.
PVETUI_PASSWORD="op://{{ .pvetui.vault }}/{{ .pvetui.item }}/password"
